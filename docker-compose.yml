services:
  printproxy.hub:
    build:
      context: .
      dockerfile: PrintProxy.Hub/Dockerfile
    ports:
      - 8181:8080
    depends_on:
      - db
  db:
   image: postgres
   restart: always
   volumes:
    -  PostgressData:/var/lib/postgresql 
   environment:
     POSTGRES_PASSWORD: postgress

volumes:
  PostgressData:
